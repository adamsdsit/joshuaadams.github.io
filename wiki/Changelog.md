# Changelog

All notable changes are recorded here. Date format: YYYY-MM-DD.

- 2025-12-10 — Modern resume redesign
  - Updated `index.html` to a single-page modern resume layout (hero, summary, experience, skills, projects, education, contact).
  - Added `resume.css` with theme variables, responsive layout, and animations.
  - Added `resume.js` for theme toggle, smooth scrolling, reveal-on-scroll, and skill bar animations.
  - Converted static skills to animated bars using `data-pct` attributes.
  - Added wiki documentation under `wiki/`.
  - Added `CNAME` file with domain `joshuaadams.dev` to enable GitHub Pages custom domain.
